# Sample-project
general configuration for cmake and debug


Example of a project structure using:
- cmake
- visual studio code
- clang format
- jenkins
- jenkins artifacts
- code coverage
- checkings scripts for: 
                        -warnings
                        -clang
                        -coverage
                        -types
                        -commit messages
